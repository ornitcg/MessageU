from utils.defines import DEFAULT_PORT

FILE_NOT_FOUND = "[ERROR] File not found"
DEFAULT_PORT_USED = f"[LOG] Default port used: {str(DEFAULT_PORT)}"

WRONG_PAYLOAD_SIZE_ERROR = "[ERROR] Payload size is smaller than expected"


